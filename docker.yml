resources:
  - name: image
    type: Image
    configuration:
      registry: docker_image
      imageName: shippabledocker/sample_node_pvt
      imageTag: master
 
pipelines:
  - name: pipeline_docker
    steps:          
      - name: bash_docker
        type: Bash
        configuration:
          inputResources:
            - name: image
        execution:
          onExecute:
            - docker images
