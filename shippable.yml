resources:
  - name: sampleRepo-inconsistent
    type: gitRepo
    repoPath: deepikasl/VT2
    configuration:
      integrationName: deepikaGithub
      #branches:
       # only: master
      #files:
       # only: '^\(.[a-z]*\)'
    version:
      sha: master
      
pipelines:
  - name: deepika_pipeline_inconsistent
    steps:
      - name: step_1_inconsistent
        type: bash
        configuration:
          affinityGroup: -deepika@#$%^&*
#           priority: 
#           timeoutSeconds: <job timeout limit>
          nodePool: test
          #chronological: true        
        triggeredBy:
          resources:
            #- file_res_1
            - sampleRepo_inconsistent
        execution:
          onExecute:
            - printenv
            - sudo apt-get install tree
            - tree $res_file_res_1_resourcePath
            - tree $res_file_res_2_resourcePath
            #- cat $STEP_TMP_DIR/fileSpecs.json
            - echo change_1
